# Vly 项目执行计划 (2026-02-07)

## 当前状态

### 项目位置
- `/Users/yanglin/.openclaw/workspace/Vly/`

### 核心问题
- `project.yml` 缺少 KSPlayer 依赖（违反核心约束）
- `VlyVideoPlayerView.swift` 使用 AVPlayer 而非 KSPlayer
- 缺少 Models 和 Services 层的实现文件

### 核心约束（必须遵守）
> "必须使用 KSPlayer + FFmpeg，做不到就放弃这个项目"

## 执行方案（已完成）

### 阶段 1：修复配置（15分钟）
- 恢复 `project.yml`，添加 KSPlayer 依赖
- 执行 `xcodegen generate`

### 阶段 2：创建数据模型（30分钟）
- `Sources/Models/Video.swift`
- `Sources/Models/Playlist.swift`
- `Sources/Models/PlaybackState.swift`

### 阶段 3：创建服务层（1小时）
- `Sources/Services/PlayerService.swift`
- `Sources/Services/PlaylistService.swift`
- `Sources/Services/FileService.swift`
- `Sources/Services/SettingsService.swift`

### 阶段 4：重写播放器（1小时）
- `VlyVideoPlayerView.swift` 改用 KSPlayer
- `ContentView.swift` 集成 PlayerService

### 阶段 5：创建视图（30分钟）
- `Sources/Views/Playlist/PlaylistView.swift`

## 用户偏好

1. **先读后做**：处理项目前必须先阅读相关文档
2. **不创建新方案**：使用现有文档，不重新发明轮子
3. **不删除文件**：未经确认不删除任何文件
4. **遵循约束**：KSPlayer 是核心约束，不能妥协

## 参考文档

- 架构设计：`docs/01-Architecture/README.md`
- 数据模型：`docs/04-Data-Models/MODELS.md`
- 服务设计：`docs/05-Services/SERVICES.md`
- 完整方案：`docs/06-Research/IMPLEMENTATION_PLAN.md`

## 下一步

等待用户确认后开始执行完整方案（约3小时15分钟）
